# Sample pg_service.conf for ACE clusters
# Service names follow the pattern <cluster>.<node>
# Configure authentication details in the cluster section and override per-node settings as needed.

[demo]
dbname=example_db
user=admin
password=changeme

[demo.n1]
host=localhost
port=5432

[demo.n2]
host=replica
port=5433
